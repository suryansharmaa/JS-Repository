const container = document.querySelector(".container");
const h1 = document.querySelector(".main-headings");
const p = document.querySelector(".sub-headings");
const button = document.querySelector(".btn");

// Provide 400px of height to Container element
console.log(container);
container.style.height = "400px";

// Change background color of container to teal
container.style.backgroundColor = "teal";

// Change the color of h1 to Skyblue
h1.style.color = "skyblue";

// Change the fontFamily of paragraph to Sans Serif
p.style.fontFamily = "sans-serif";

// Change the color of paragraph to white
p.style.color = "white";

// Change the color of button to pink
button.style.color = "pink";
